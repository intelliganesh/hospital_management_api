<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Consultation Bill - Lovable</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            margin: 40px;
            line-height: 1.6;
        }

        h1,
        h2,
        h3 {
            margin-bottom: 10px;
        }

        .section {
            margin-bottom: 25px;
        }

        .label {
            font-weight: bold;
        }

        .info-table,
        .bill-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        .info-table td {
            padding: 4px 8px;
            vertical-align: top;
        }

        .bill-table th,
        .bill-table td {
            border: 1px solid #ccc;
            padding: 8px;
            text-align: left;
        }

        .bill-table th {
            background-color: #f0f0f0;
        }

        .total {
            text-align: right;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div style="display: flex; align-items: center; justify-content: center;">
        <p style="text-align: center"><strong style="font-size: 24px">{{ env('APP_NAME') }}</strong><br>
            {{ env('HOSPITAL_ADDRESS') }}
        </p>
    </div>

    <div class="section" style="display: flex; align-items: start; justify-content: space-between;">
        <p><strong>Invoice No:</strong> {{ $invoice_number }}<br>
            {{-- <strong>Date:</strong> June 10, 2025<br>
            <strong>Time:</strong> 10:30 AM --}}
        </p>
    </div>
    <hr>
    <div class="section">
        <h3>Patient Information</h3>
        <div style="display: flex; align-items: start; justify-content: space-between;">
            <table class="info-table">
                <tr>
                    <td class="label">Name:</td>
                    <td>{{ $patient_name }}</td>
                </tr>
                <tr>
                    <td class="label">Email:</td>
                    <td>{{ $patient_email }}</td>
                </tr>
                <tr>
                    <td class="label">Phone:</td>
                    <td>{{ $patient_number }}</td>
                </tr>
            </table>
        </div>
    </div>
    <div class="section">
        <h3>Doctor Information</h3>
        <div style="display: flex; align-items: start; justify-content: space-between;">
            <table class="info-table">
                <tr>
                    <td class="label">Name:</td>
                    <td>{{ $doctor_name }}</td>
                </tr>
                <tr>
                    <td class="label">Email:</td>
                    <td>{{ $doctor_email }}</td>
                </tr>
                <tr>
                    <td class="label">Phone:</td>
                    <td>{{ $doctor_phone }}</td>
                </tr>
                <tr>
                    <td class="label">Signature:</td>
                    <td>
                        <hr style="margin-top: 30px; " />
                    </td>
                </tr>
            </table>
        </div>
    </div>
    <div class="section">
        <h3>Bill Details</h3>
        <table class="bill-table">
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Rate (Rs)</th>
                    <th>Total Amount (Rs)</th>
                </tr>
            </thead>
            <tbody>
                @php
                    $totalAmount = 0;
                @endphp
                @if ($type == 'First Visit')
                    @php
                        $totalAmount += $enroll_fees;
                    @endphp
                    <tr>
                        <td>Enrolement Fee</td>
                        <td>{{ $enroll_fees }}</td>
                        <td>{{ $enroll_fees }}</td>

                    </tr>
                @endif
                <tr>
                    <td>Consultation Fee</td>
                    <td>{{ $fees }}</td>
                    <td>{{ $fees }}</td>
                </tr>
                @if (count($test) > 0)
                    <tr>
                        @foreach ($test as $item)
                            @php
                                $totalAmount += $item->test_price + $item->tax_price;
                            @endphp
                            <td>{{ $item->test_name }} ({{ $item->test_number }})</td>
                            <td>{{ $item->test_price }} + {{ $item->tax_price }} (tax) </td>
                            <td>{{ $item->test_price + $item->tax_price }}</td>
                        @endforeach
                    </tr>
                @endif
                @if (count($paymentArray) > 0)
                    @foreach ($paymentArray as $item)
                        <tr>
                            <td>{{ $item['amount_for'] }}</td>
                            <td>{{ $item['amount'] }}</td>
                            <td>{{ $item['amount'] }}</td>
                        </tr>
                    @endforeach
                @endif
            </tbody>
        </table>

        {{-- <p class="total">Total Amount: Rs {{ $totalAmount + $fees }}</p> --}}
        <p class="total">Total Amount: Rs {{ $total_amount }}</p>
        <p class="total">Balance Amount: Rs {{ $balanced_amount }}</p>
    </div>
    <hr>
    <div class="section">
        <h3>Payment Information</h3>
        <table width="100%">
            <tr>
                <td align="left" style="vertical-align: top;">
                    <table>
                        <tr>
                            <td class="label"><strong>Payment Method</strong>: {{ $payment_type }}</td>
                        </tr>
                        <tr>
                            <td class="label"><strong>Transaction ID</strong> :{{ $transaction_id ?? '' }}</td>
                        </tr>
                    </table>
                </td>
            </tr>
            {{-- <tr>
                <td align="right" style="vertical-align: top;">
                    <table>
                        <tr>
                            <td class="label"><strong>Authorized Signature</strong>:
                                <hr>
                            </td>
                        </tr>
                    </table>
                </td>
            </tr> --}}
        </table>
    </div>

    <br><br><br>

    {{-- <div style="display: flex; align-items: center; justify-content: center;">
        <p style="text-align: center"><strong>{{ env('APP_NAME') }}</strong><br>
            Thank you for choosing {{ env('APP_NAME') }} for your healthcare needs.<br>
            For any queries regarding this bill, please contact us at {{ env('HOSPITAL_EMAIL') }}</p>
    </div> --}}

</body>

</html>

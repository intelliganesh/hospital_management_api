<div id="myModal_{{ $index }}" class="modal">
    <div class="modal-content">
        <div class="sticky">
            <span id="close_{{ $index }}" onclick="closeModal(this)" class="close"
                data-modelid="{{ $index }}">&times;</span>
        </div>
        <p id="subject_{{ $index }}" style="word-break:break-word"></p>
    </div>

</div>
